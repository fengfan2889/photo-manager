# 开发日志 - PhotoManager

## 日志格式说明

每次开发会话记录以下信息：
- **日期时间**: 会话时间
- **任务**: 本次开发目标
- **变更**: 修改了哪些文件
- **状态**: 完成/进行中/阻塞
- **备注**: 问题或待办

---

## 开发记录

### 2026-04-04 20:34
- **任务**: 项目初始化，需求分析，目录结构创建
- **变更**: 
  - 创建 `doc/设计文档.md` (v0.1)
  - 创建 `doc/开发计划.md`
  - 创建 `doc/开发日志.md`
  - 移动项目到 `F:\jyw\study\python\photo-manager\`
- **状态**: 完成
- **备注**: 基础版本，CLI 架构

### 2026-04-04 21:00
- **任务**: 架构升级，按 fin-agent-desktop 模式重新设计
- **变更**: 
  - 更新 `doc/设计文档.md` (v0.2)
    - 技术栈：Electron + React + Tailwind + Python
    - 架构：参考 fin-agent-desktop 主进程 + preload + 渲染进程
    - IPC 通信设计
    - 双入口模式（桌面 + 浏览器）
  - 更新 `doc/开发计划.md`
    - Phase 1-6 任务重新规划
    - 基于 Electron 架构的依赖
- **状态**: 完成
- **备注**: 
  - 参考项目：fin-agent-desktop (GitHub)
  - 等待用户确认设计后开始开发

### 2026-04-04 21:30
- **任务**: 添加日志模块设计
- **变更**: 
  - 更新 `doc/设计文档.md` (v0.3)
    - 添加 `loguru` (Python) 和 `electron-log` 技术依赖
    - 添加日志级别定义（TRACE/DEBUG/INFO/SUCCESS/WARNING/ERROR/FATAL）
    - 添加统一日志格式规范
    - 添加 Python 后端日志模块设计 (`python/src/logger.py`)
    - 添加 Electron 主进程日志模块设计 (`electron/src/main/logger.ts`)
    - 添加 React 前端日志模块设计 (`electron/src/renderer/src/utils/logger.ts`)
    - 添加 IPC 日志传递流程图
    - 添加日志级别控制方案
    - 更新目录结构（新增 logger 文件）
- **状态**: 完成
- **备注**: 
  - 前端日志通过 console 输出，可选发送到主进程记录到文件
  - Python 日志通过 stdout JSON 格式传递给主进程
  - 支持开发时详细日志，生产时精简日志

### 2026-04-04 22:00
- **任务**: 数据库表设计规范化，拆分表设计文档
- **变更**: 
  - 创建 `doc/数据库设计.md`
    - 7 张表设计：photo_info、tag_info、photo_tag、face_info、face_subject、organize_log、setting_info
    - 两段式命名规范（模块_表内容）
    - 完整 ER 图
    - 详细字段定义和索引
    - 完整建表 SQL
    - 预设数据（标签、配置）
  - 更新 `doc/设计文档.md` (v0.4)
    - 数据模型部分精简，引用数据库设计文档
- **状态**: 完成
- **备注**: 
  - face_subject 表用于人脸聚类和人物命名
  - setting_info 表存储所有配置，支持动态修改
  - organize_log 表记录每次整理操作，便于追溯

### 2026-04-04 22:30
- **任务**: 模块化重构，规范化命名，添加设计原则
- **变更**: 
  - 更新 `doc/数据库设计.md` (v1.1.0)
    - 表名前缀：`photo_xxx` / `sys_xxx`
    - 字段命名：snake_case
    - 索引命名：`idx_` / `uq_`
    - 表重命名：`tag_info` → `photo_tag`
    - 表重命名：`organize_log` → `photo_log`
    - 表重命名：`setting_info` → `sys_setting`
    - 表重命名：`sys_log`（新增系统日志表）
    - 字段重命名：`photo_lat/lon` → `latitude/longitude`
  - 更新 `doc/设计文档.md` (v0.5)
    - 添加「设计原则」章节
    - 模块化原则
    - 命名规范（表、字段、索引）
    - 公共字段规范
    - 关系设计原则
    - 字段设计原则
    - 索引设计原则
    - 参考规范链接
- **状态**: 完成
- **备注**: 
  - 参考：Drygast 数据库命名规范
  - 系统仅两个模块：photo（照片）、sys（系统）

### 2026-04-04 23:00
- **任务**: 精简设计原则，优化表命名
- **变更**: 
  - 更新 `doc/设计文档.md` (v0.6)
    - 设计原则精简为3点：表命名、字段命名、公共字段
    - 表命名支持多段式：`photo_face_detect`、`photo_face_subject`、`sys_log_operation`
  - 更新 `doc/数据库设计.md` (v1.2.0)
    - 表重命名：`photo_face` → `photo_face_detect`
    - 表重命名：`photo_log` → `photo_log_operation`
    - 表重命名：`sys_log` → `sys_log_operation`
- **状态**: 完成
- **备注**: 
  - 表名通过多段式命名体现具体内容
  - 如 `photo_face_detect` 明确表示"照片-人脸-检测"

### 2026-04-04 23:10
- **任务**: Phase 1 项目骨架搭建
- **变更**: 
  - 创建 `electron/` 目录结构（electron-vite 标准）
    - `src/main/index.ts` - Electron 主进程入口
    - `src/preload/index.ts` - IPC API 暴露
    - `src/renderer/src/` - React 前端
    - `package.json`, `tsconfig.json`, `electron.vite.config.ts` 等配置文件
  - 创建 `python/` 目录结构
    - `src/logger.py` - 日志模块（loguru）
    - `src/database.py` - 数据库模块（SQLite 建表 + CRUD）
    - `requirements.txt` - Python 依赖
  - 创建页面组件
    - `pages/Gallery.tsx` - 照片库页面
    - `pages/Organizer.tsx` - 整理向导页面
    - `pages/Annotation.tsx` - 照片标注页面
    - `pages/Settings.tsx` - 设置页面
  - 创建前端日志模块
    - `utils/logger.ts` - 前端日志工具
- **状态**: 完成
- **备注**: 
  - 项目骨架搭建完成，参考 fin-agent-desktop 架构
  - Python 数据库模块包含 8 张表的建表 SQL
  - IPC API 接口已定义（待实现 Python 端逻辑）
  - 下一步：实现 Python 端核心逻辑（照片整理、人脸检测）

### 2026-04-05 00:00
- **任务**: Phase 2 Python 核心模块实现
- **变更**: 
  - 创建 `python/src/exif_parser.py`
    - `ExifParser` 类：解析图片 EXIF 数据
    - 支持字段：拍摄时间、相机信息、GPS 坐标等
    - `get_file_info()` 函数：获取文件基本信息
    - `resolve_taken_time()` 函数：时间解析（带 fallback）
  - 创建 `python/src/organizer.py`
    - `PhotoOrganizer` 类：照片整理核心逻辑
    - 支持模式：copy / move / link
    - 按时间自动组织目录：`{year}/{date}/`
    - 文件名冲突自动处理
  - 创建 `python/src/face_detector.py`
    - `FaceDetector` 类：使用 face_recognition 检测人脸
    - `FaceMatcher` 类：人脸匹配和聚类
    - 支持离线人脸检测
  - 创建 `python/src/json_exporter.py`
    - `JsonExporter` 类：导出数据库到 JSON
    - `JsonImporter` 类：从 JSON 导入数据库
    - 支持数据合并或覆盖
  - 创建 `python/src/main.py`
    - CLI 入口：organize / export / import / init / info
    - `IPCServer` 类：与 Electron IPC 通信
    - 支持所有照片操作命令
  - 更新 `electron/src/main/index.ts`
    - 实现 Python 子进程管理
    - 实现 IPC 请求/响应队列
    - 打通 Electron → Python 调用链路
- **状态**: 完成
- **备注**: 
  - Python 端核心功能全部实现
  - 人脸检测依赖 face_recognition（需安装 dlib）
  - IPC 通信基于 stdin/stdout JSON
  - 下一步：测试运行，安装依赖

### 2026-04-05 00:30
- **任务**: Phase 3 完善功能
- **变更**: 
  - 创建 `python/src/photo_repo.py`
    - `PhotoRepo` 类：照片 CRUD
    - `TagRepo` 类：标签 CRUD
    - `FaceRepo` 类：人脸 CRUD
    - `SubjectRepo` 类：人物主题 CRUD
  - 更新 `python/src/main.py`
    - 添加标签管理 IPC 命令（get-tags, create-tag, delete-tag 等）
    - 添加人脸管理 IPC 命令（get-faces, name-face, get-subjects 等）
    - 使用 PhotoRepo 等仓库类
  - 创建前端组件
    - `components/Layout.tsx` - 顶部导航栏布局
    - `components/PhotoGrid.tsx` - 照片网格组件
  - 创建标签管理页面
    - `pages/Tags.tsx` - 标签列表、添加、删除
  - 更新页面
    - `Gallery.tsx` - 使用 PhotoGrid 组件、搜索功能
    - `Annotation.tsx` - 人脸检测、标签管理
    - `Settings.tsx` - 设置页面
  - 更新 `preload/index.ts`
    - 添加标签相关 API
    - 更新类型定义
  - 添加打包配置
    - `electron-builder.json` - Windows/Mac/Linux 打包
- **状态**: 完成
- **备注**: 
  - 前后端功能基本完善
  - 支持照片浏览、整理、标注、标签管理
  - 打包配置完成，可生成安装包

### 2026-04-05 01:00
- **任务**: 人脸检测库替换（face_recognition → insightface）
- **变更**: 
  - 更新 `python/requirements.txt`
    - 移除 `face-recognition>=1.3.0`
    - 添加 `insightface>=0.7.0`
    - 添加 `onnxruntime>=1.16.0`
  - 更新 `python/src/face_detector.py`
    - 使用 `insightface` 替代 `face_recognition`
    - 使用 `buffalo_l` 模型
    - 保持 API 兼容
    - 额外提取年龄、性别等属性
- **状态**: 完成
- **备注**: 
  - `face_recognition` 依赖 `dlib`，在 Windows 中文环境编译失败
  - `insightface` 纯 Python 实现，无需编译，安装简单
  - `insightface` 精度更高，模型更小

### 2026-04-05 01:15
- **任务**: 移除人脸检测 Python 依赖，改用占位实现
- **变更**: 
  - 更新 `python/requirements.txt`
    - 移除 `insightface>=0.7.0`
    - 移除 `onnxruntime>=1.16.0`
  - 更新 `python/src/face_detector.py`
    - 改写为占位实现
    - 所有方法返回空结果/None
    - 添加警告日志说明功能已禁用
- **状态**: 完成
- **备注**: 
  - 人脸检测依赖 C++ 编译器（VS Build Tools），安装失败
  - 暂时禁用 Python 人脸检测
  - 后续可通过前端 face-api.js 或安装 VS Build Tools 实现

### 2026-04-05 17:05
- **任务**: 修复 Python 导入错误和日志 stacktrace
- **变更**: 
  - `electron/src/main/index.ts`
    - Python 启动方式改为模块方式：`python -m src.main --ipc`
    - cwd 改为 `python/` 目录
  - `python/src/logger.py`
    - 日志格式添加 `{exception}` 占位符
    - 添加 `backtrace=True` 和 `diagnose=True`
  - `python/src/face_detector.py`
    - 移除警告日志（用户选择保留警告）
- **状态**: 完成
- **备注**: 
  - 相对导入需要用 `-m` 模块方式运行
  - loguru 默认不输出异常信息，需在格式中添加 `{exception}`

### 2026-04-05 19:10
- **任务**: 添加「记住整理配置」功能
- **变更**: 
  - `python/src/database.py`
    - 添加 `organize_source` 配置项
  - `python/src/setting_repo.py`（新建）
    - 配置 CRUD 操作
    - `get()` / `set()` 单项配置
    - `get_group()` / `set_group()` 批量配置
    - `get_organize_config()` / `save_organize_config()` 整理配置
  - `python/src/main.py`
    - 导入 SettingRepo
    - 添加 `get-settings` / `save-settings` IPC 命令
  - `electron/src/main/index.ts`
    - 添加 `get-settings` / `save-settings` IPC handlers
  - `electron/src/preload/index.ts`
    - 添加 `OrganizeSettings` 类型
    - 添加 `getSettings()` / `saveSettings()` API
  - `electron/src/renderer/src/pages/Organizer.tsx`
    - 初始化时读取配置
    - 添加「保存配置」按钮
    - 整理前自动保存配置
- **状态**: 完成
- **备注**: 
  - 配置存储在 `sys_setting` 表
  - 记住源目录、目标目录、整理模式

### 2026-04-05 21:35
- **任务**: 修复 IPC 通信问题
- **问题**: 
  - Python 返回完整响应 `{success, data, id}`，但 Electron 端只返回 `data` 部分
  - 导致所有检查 `result?.success` 的代码失败
  - 响应没有正确传递到前端
- **变更**: 
  - `electron/src/main/index.ts`
    - 添加 Python 就绪信号检测，等待 `{"ready": true}` 后再设置 `pythonReady`
    - 修复 `handlePythonResponse` 返回完整响应而非仅 `data`
    - 添加详细调试日志（stdout、JSON 解析、响应处理）
  - `python/src/main.py`
    - IPC 模式启动时发送就绪信号
    - 添加调试日志（收到命令、执行结果、JSON 输出）
    - 响应中添加 `id` 字段
  - `python/src/logger.py`
    - 修复日志输出到 stderr（供 Electron 捕获）
  - `electron/src/renderer/src/pages/Gallery.tsx`
    - 兼容两种返回格式
- **状态**: 完成
- **备注**: 
  - IPC 通信协议：Python 返回 `{success, data, id}`，Electron 透传
  - React StrictMode 会导致开发模式下的重复渲染
  - 日志级别改为 DEBUG 以查看详细通信

### 2026-04-05 21:40
- **任务**: 修复照片整理全部失败的问题
- **问题**: 
  - 错误：`unsupported operand type(s) for /: 'WindowsPath' and 'NoneType'`
  - 原因：`_generate_target_path` 返回 `(target_dir, None)`，文件名是 None
  - 导致 `target_dir / None` 报错
- **变更**: 
  - `python/src/organizer.py`
    - 修改 `_generate_target_path` 签名，传入 `file_path` 参数
    - 返回正确的文件名而非 None
- **状态**: 完成
- **备注**: 
  - 之前代码中 `return target_dir, None` 忘记修改

### 2026-04-05 21:50
- **任务**: 添加移动日志功能
- **变更**: 
  - `python/src/organizer.py`
    - 添加 `move_logs` 列表记录每次移动
    - `_process_file` 中记录每次移动的源、目标、原因
    - 整理完成后保存到 `move_log.json`
- **状态**: 完成
- **备注**: 
  - 日志格式：`{timestamp, source, dest, mode, reason}`
  - 保存位置：目标目录下的 `move_log.json`

### 2026-04-05 22:00
- **任务**: 添加视频文件支持
- **变更**: 
  - `python/src/exif_parser.py`
    - 添加 `VIDEO_FORMATS` 支持：`.mp4`, `.mov`, `.avi`, `.mkv`, `.webm`, `.flv`, `.wmv`, `.m4v`
    - 添加 `get_video_creation_time()` 使用 ffprobe 获取视频创建时间
    - 添加 `is_video_file()` 判断文件是否为视频
  - `python/src/organizer.py`
    - 添加 `VIDEO_FORMATS` 和 `ALL_FORMATS`
    - `scan()` 改为扫描所有媒体文件
    - `_process_file()` 根据文件类型选择解析方式（图片用 EXIF，视频用 ffprobe）
    - 解析结果中添加 `type` 字段区分图片和视频
- **状态**: 完成
- **备注**: 
  - 需要安装 ffmpeg 才能提取视频时间
  - 命令：`choco install ffmpeg` 或从 https://ffmpeg.org 下载

### 2026-04-05 22:10
- **任务**: 整理后保存到数据库
- **问题**: 整理完成后查询无数据，因为没有保存到数据库
- **变更**: 
  - `python/src/organizer.py`
    - `PhotoOrganizer` 添加 `photo_repo` 参数
    - 添加 `_save_to_database()` 方法保存到 `photo_info` 表
    - 整理完成后调用保存方法
  - `python/src/main.py`
    - `_organize()` 传入 `photo_repo` 参数
- **状态**: 完成
- **备注**: 
  - 每次整理后，照片信息保存到数据库
  - 包括文件路径、哈希、时间、相机信息等

### 2026-04-05 22:20
- **任务**: 优化列表返回日志
- **变更**: 
  - `python/src/main.py`
    - 对列表结果，只记录条数 `returned X items`
    - 其他结果记录 success 状态
  - 日志级别改回 INFO
- **状态**: 完成

### 2026-04-05 22:35
- **任务**: 完善 IPC handlers
- **问题**: 
  - `get-tags`, `create-tag`, `delete-tag`, `get-photo-tags`, `name-face` 没有注册 handler
  - `photo_repo` 未传入导致不保存数据库
- **变更**: 
  - `electron/src/main/index.ts`
    - 添加 `get-tags`, `create-tag`, `delete-tag`, `get-photo-tags`, `name-face` handlers
  - `python/src/organizer.py`
    - 添加 `photo_repo` 为空时的警告日志
- **状态**: 完成
