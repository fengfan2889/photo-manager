# 当前工作

---
 
## 工作记录

### 2026-05-06 11:44 - 修复 Portable 版运行问题

**任务**: 修复 Portable 版运行时日志路径和 Python ready 检查问题

**文件变更**:
- `electron/src/main/index.ts`:
  - 修改 pythonExec 函数：初始化命令（get-current-dir）不需要等待 ready 状态
  - 添加 initCommands 白名单
  - 修复日志路径：使用 app.getAppPath() 作为日志目录
  - 确保日志目录存在（mkdirSync）

**问题分析**:
- 日志显示：发送 get-current-dir 命令时 pythonReady 还是 false
- 但后来收到了 {"ready": true} 信号
- 竞态条件：ready 信号还没设置，命令就发送了

**解决方案**:
- 初始命令在 Python 进程启动后即可用，不需要等待 ready 信号
- 其他命令（如 query-photos）才需要等待 ready
- 日志放到应用目录下的 logs/ 中

**验证结果**: 重新打包后测试
