# 设计文档 - PhotoManager

## 项目概述

- **项目名称**: PhotoManager
- **创建日期**: 2026-04-04
- **项目描述**: 轻量级本地照片管理软件，基于 Electron + Python，桌面端 + 浏览器双入口

---

## 技术栈

### 桌面端（Electron）

| 组件 | 技术 | 版本 |
|------|------|------|
| 桌面框架 | Electron | ^28.0.0 |
| 构建工具 | electron-vite | ^2.0.0 |
| 前端框架 | React | ^18.2.0 |
| 路由 | React Router | ^7.10.1 |
| 样式 | Tailwind CSS | ^3.4.0 |
| 图标 | lucide-react | ^0.300.0 |
| 打包 | electron-builder | ^24.9.1 |
| 语言 | TypeScript | ^5.3.3 |
| 日志 | electron-log | ^5.0.0 |

### Python 后端

| 组件 | 技术 | 说明 |
|------|------|------|
| 核心语言 | Python | 3.10+ |
| EXIF 解析 | exifread / piexif | 图片元数据 |
| 图片处理 | Pillow | 缩略图、图像操作 |
| 数据库 | sqlite3 | 内置，无需安装 |
| 人脸检测 | (占位) | 需要 VS Build Tools |
| JSON | orjson | 高性能序列化 |
| 日志 | loguru | ^0.7.0 |
| 视频时间 | ffprobe | 需要安装 ffmpeg |

---

## 架构设计

### 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        Electron 主进程                          │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                   IPC Bridge (preload)                   │    │
│  │   window.electronAPI.invoke('organize', {...})          │    │
│  │   window.electronAPI.invoke('query-photos', {...})     │    │
│  └─────────────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                   Python Subprocess                      │    │
│  │   child_process.spawn('python', ['main.py', ...])      │    │
│  └─────────────────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────────────────┤
│                        Electron 渲染进程                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │
│  │   照片浏览    │  │   照片整理    │  │   标注管理    │           │
│  │  (Gallery)   │  │ (Organizer) │  │ (Annotation) │           │
│  └──────────────┘  └──────────────┘  └──────────────┘           │
│  ┌──────────────┐  ┌──────────────┐                             │
│  │   搜索过滤    │  │   设置中心    │                             │
│  │  (Search)    │  │ (Settings)   │                             │
│  └──────────────┘  └──────────────┘                             │
├─────────────────────────────────────────────────────────────────┤
│                         Python 后端                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ EXIF Parser  │  │ TimeResolver │  │ FileOps      │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  Database    │  │ FaceDetector │  │ JsonExporter │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
```

### 双入口模式

| 入口 | 说明 | 端口 |
|------|------|------|
| **桌面窗口** | Electron 内嵌 Chromium | 内置 |
| **浏览器访问** | 可选启动本地 HTTP 服务 | localhost:3000 |

> 参考 fin-agent-desktop：Electron 主进程启动时可选启动一个本地 HTTP 服务，供浏览器访问

---

## 目录结构

```
photo-manager/
├── electron/                    # Electron 项目
│   ├── src/
│   │   ├── main/              # Electron 主进程
│   │   │   ├── index.ts       # 主进程入口
│   │   │   ├── ipc.ts         # IPC 处理器
│   │   │   ├── python.ts      # Python 进程管理
│   │   │   └── logger.ts      # 日志管理
│   │   ├── preload/           # 预加载脚本
│   │   │   └── index.ts      # 暴露 API 给渲染进程
│   │   └── renderer/         # React 前端
│   │       ├── src/
│   │       │   ├── pages/    # 页面组件
│   │       │   ├── components/  # 通用组件
│   │       │   ├── hooks/      # React Hooks
│   │       │   ├── utils/
│   │       │   │   └── logger.ts  # 前端日志
│   │       │   ├── App.tsx
│   │       │   └── main.tsx
│   │       └── index.html
│   ├── electron.vite.config.ts
│   ├── tsconfig.json
│   └── package.json
│
├── python/                     # Python 后端
│   ├── src/
│   │   ├── __init__.py
│   │   ├── main.py           # CLI 入口 + API 模式
│   │   ├── logger.py         # 日志管理 (loguru)
│   │   ├── organizer.py      # 整理逻辑
│   │   ├── database.py       # SQLite 操作
│   │   ├── exif_parser.py   # EXIF 解析
│   │   ├── face_detector.py # 人脸检测
│   │   └── json_exporter.py # JSON 导出
│   ├── requirements.txt
│   └── setup.py
│
├── doc/                       # 文档
│   ├── 设计文档.md
│   ├── 开发计划.md
│   └── 开发日志.md
│
├── resources/                 # 资源文件
│   └── icon.png
│
├── electron-builder.json      # 打包配置
├── package.json               # 根 package.json (Electron 启动用)
└── AGENTS.md
```

---

## IPC 通信设计

### 暴露给前端的方法

```typescript
// preload/index.ts
import { contextBridge, ipcRenderer } from 'electron'

contextBridge.exposeInMainWorld('electronAPI', {
  // 照片操作
  organizePhotos: (options: OrganizeOptions) => 
    ipcRenderer.invoke('organize-photos', options),
  
  queryPhotos: (filters: PhotoFilters) => 
    ipcRenderer.invoke('query-photos', filters),
  
  getPhoto: (id: number) => 
    ipcRenderer.invoke('get-photo', id),
  
  // 标注操作
  setRating: (photoId: number, rating: number) => 
    ipcRenderer.invoke('set-rating', photoId, rating),
  
  addTag: (photoId: number, tagName: string) => 
    ipcRenderer.invoke('add-tag', photoId, tagName),
  
  removeTag: (photoId: number, tagName: string) => 
    ipcRenderer.invoke('remove-tag', photoId, tagName),
  
  detectFaces: (photoId: number) => 
    ipcRenderer.invoke('detect-faces', photoId),
  
  nameFace: (photoId: number, faceIndex: number, name: string) => 
    ipcRenderer.invoke('name-face', photoId, faceIndex, name),
  
  // 数据操作
  exportJson: (outputPath: string) => 
    ipcRenderer.invoke('export-json', outputPath),
  
  importFromJson: (inputPath: string) => 
    ipcRenderer.invoke('import-json', inputPath),
  
  // 系统
  selectDirectory: () => 
    ipcRenderer.invoke('select-directory'),
  
  openInExplorer: (path: string) => 
    ipcRenderer.invoke('open-in-explorer', path),
  
  // 进度回调
  onProgress: (callback: (progress: Progress) => void) => 
    ipcRenderer.on('organize-progress', callback),
})
```

### 主进程 IPC 处理器

```typescript
// main/ipc.ts
import { ipcMain } from 'electron'

export function setupIpcHandlers(): void {
  ipcMain.handle('organize-photos', async (event, options) => {
    return python.exec('organize', options)
  })
  
  ipcMain.handle('query-photos', async (event, filters) => {
    return python.exec('query', filters)
  })
  
  // ... 其他 handlers
}
```

---

## IPC 命令清单

### 通信协议

请求格式：
```json
{"command": "命令名", "args": {"参数": "值"}}
```

响应格式：
```json
{"success": true, "data": {...}, "id": "请求ID"}
{"success": false, "error": "错误信息", "id": "请求ID"}
```

### 命令列表

| 命令 | 参数 | 返回 | 说明 |
|------|------|------|------|
| `organize` | `source`, `dest`, `mode`, `include_unknown` | `OrganizeResult` | 整理照片 |
| `query-photos` | `limit`, `offset`, `rating`, `search` | `Photo[]` | 查询照片列表 |
| `get-photo` | `id` | `Photo` | 获取单张照片 |
| `set-rating` | `photo_id`, `rating` | `boolean` | 设置星级 |
| `get-tags` | - | `Tag[]` | 获取所有标签 |
| `create-tag` | `name`, `color` | `tag_id` | 创建标签 |
| `delete-tag` | `id` | `boolean` | 删除标签 |
| `add-tag` | `photo_id`, `tag_id` | `boolean` | 添加标签 |
| `remove-tag` | `photo_id`, `tag_id` | `boolean` | 移除标签 |
| `detect-faces` | `photo_id` | `Face[]` | 检测人脸 |
| `export-json` | `output_path` | `boolean` | 导出 JSON |
| `import-json` | `input_path` | `boolean` | 导入 JSON |
| `get-settings` | `group` | `OrganizeSettings` | 获取配置 |
| `save-settings` | `OrganizeSettings` | `boolean` | 保存配置 |

### 通用 Python 执行器

除专用命令外，还可通过 `python-exec` 执行任意命令：

```typescript
// 前端
const result = await window.electronAPI.pythonExec('query-photos', { limit: 100 })

// 等同于
const result = await ipcRenderer.invoke('python-exec', 'query-photos', { limit: 100 })
```

---

## 功能实现状态

### 已完成

| 功能 | 实现 | 说明 |
|------|------|------|
| 图片整理 | ✅ | copy/move/link，按拍摄时间整理 |
| 视频支持 | ✅ | 需要 ffmpeg |
| 数据库存储 | ✅ | SQLite，8 张表 |
| JSON 导出/导入 | ✅ | 每次整理后可导出 |
| 记住配置 | ✅ | 保存源/目标目录、整理模式 |
| hash 去重 | ✅ | 根据 file_hash 避免重复 |
| 移动日志 | ✅ | move_log.json |
| 星级评分 | ✅ | 0-5 级 |
| 标签管理 | ✅ | CRUD + 颜色 |
| Gallery 页面 | ✅ | 照片网格显示 |
| Annotation 页面 | ✅ | 照片详情、标注 |
| Settings 页面 | ✅ | 整理配置 |

### 待实现

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 搜索过滤 | P1 | Gallery 页面搜索功能 |
| 人脸检测 | P2 | 自动检测人脸（需要 VS C++ Build Tools） |
| Windows 打包 | P1 | electron-builder 配置 |
| Web 入口 | P2 | 可选浏览器访问 |

---

## 新需求设计分析

### 1. 搜索过滤 - Gallery 页面搜索功能

#### 当前实现

- 前端搜索框已存在 (`Gallery.tsx` 第 71-78 行)
- 后端 `PhotoRepo.get_list()` 支持 `search` 参数，仅搜索 `file_name` 和 `camera_model`

#### 扩展建议

| 搜索字段 | 实现方式 | 优先级 |
|----------|----------|--------|
| 文件名 | 已支持 | P0 |
| 拍摄时间 | 扩展 SQL WHERE | P1 |
| 标签 | 扩展 SQL JOIN | P1 |
| 相机型号 | 已支持 | P0 |
| 评级 | 已有 filter 参数 | P0 |
| 日期范围 | 已有 date_from/date_to | P0 |

#### 推荐方案

```python
# photo_repo.py 扩展搜索字段
def get_list(..., search: str = None):
    if search:
        sql += """
        AND (
            p.file_name LIKE ? 
            OR p.camera_model LIKE ? 
            OR p.camera_make LIKE ?
            OR p.taken_at LIKE ?
        )"""
        params.extend([f'%{search}%'] * 4)
```

---

### 2. Web 入口 - 可选浏览器访问

#### 需求场景

- 用户点击链接直接在系统浏览器中打开照片详情页
- 支持自定义协议深度链接 (如 `photomanager://photo/123`)
- Web URL 分享功能

#### 推荐实现

**方案 A: shell.openExternal (简单)**

```javascript
// main.js
const { shell } = require('electron');
shell.openExternal('https://your-app.com/photo/123');
```

**方案 B: 自定义协议 (深度链接)**

```
photomanager://photo/123?token=xxx
```

| 组件 | 实现 |
|------|------|
| 注册协议 | `app.setAsDefaultProtocolClient('photomanager')` |
| 处理 URL | `app.on('open-url', (event, url) => {...})` |
| macOS | AppDelegate 或 open-url 事件 |
| Windows | single-instance + argv 处理 |
| 打包配置 | electron-builder protocols 配置 |

**安全性**

- 只允许 `http:` 和 `https:` 协议
- URL 参数白名单验证
- 避免传递未信任内容给 shell.openExternal

**证据**

- Electron shell.openExternal: https://github.com/electron/electron/blob/main/docs/api/shell.md
- Deep linking: https://github.com/electron/electron/blob/main/docs/api/app.md

---

### 3. 人脸检测 - 自动检测人脸

#### 约束

- 当前无法调试，缺少 VS C++ Build Tools
- 需要无 native build 依赖的纯 JS 方案

#### 推荐方案

**方案 A: face-api.js (推荐)**

| 特性 | 说明 |
|------|------|
| 运行环境 | 浏览器 renderer (纯 JS + TensorFlow.js) |
| 模型 | TinyFaceDetector (190KB) / SSD MobileNet |
| 依赖 | npm install face-api.js |
| 输出 | 68 点人脸关键点 + 边界框 |
| 性能 | TinyFaceDetector 实时，SSD 精度高 |

```javascript
// 使用示例
import * as faceapi from 'face-api.js';

const models = await faceapi.nets.tinyFaceDetector.loadFromUri('/models');
const detections = await faceapi.detectAllFaces(image, new faceapi.TinyFaceDetectorOptions());
// 提取人脸区域生成缩略图
```

**方案 B: MediaPipe Face Detection**

| 特性 | 说明 |
|------|------|
| 运行环境 | 浏览器 (WebAssembly) |
| 模型 | BlazeFace |
| 依赖 | @mediapipe/face_detection |
| 性能 | 更轻量，精度略低 |

**方案 C: OpenCV.js (备选)**

| 特性 | 说明 |
|------|------|
| 运行环境 | 浏览器 (WebAssembly) |
| 检测器 | Haar Cascade / DNN |
| 大小 | ~8MB (较大) |

**推荐决策**

| 方案 | 准确度 | 性能 | 依赖 | 推荐 |
|------|--------|------|------|------|
| face-api.js (SSD) | 高 | 中 | ~10MB | ⭐ 首选 |
| face-api.js (Tiny) | 中 | 高 | ~190KB | 首选 |
| MediaPipe | 中 | 高 | ~1MB | P1 |
| OpenCV.js | 中 | 低 | ~8MB | 备选 |

**数据库扩展**

```sql
-- 新增人脸表
CREATE TABLE photo_face (
    id INTEGER PRIMARY KEY,
    photo_id INTEGER REFERENCES photo_info(id),
    face_index INTEGER,  -- 同一照片中的第几张人脸
    bbox_x REAL,
    bbox_y REAL,
    bbox_width REAL,
    bbox_height REAL,
    confidence REAL,
    thumb_path TEXT,  -- 人脸缩略图路径
    embedding BLOB,    -- 人脸特征向量（可选）
    created_at TEXT
);

-- photo_info 新增字段
ALTER TABLE photo_info ADD COLUMN face_count INTEGER DEFAULT 0;
```

**前端流程**

1. 导入照片时触发检测（或后台异步）
2. 检测人脸 → 提取人脸区域 → 生成缩略图 → 保存到 `thumbs/faces/`
3. 人脸管理页面显示检测到的人脸

---

---

## 新功能设计：导入时自动提取目录标签

### 需求描述

在导入照片时，如果图片所在目录（可能多层）上有分隔符，将除日期之外的内容分割为合适的标签放入图片中：
- 例如：`2024-01-15/vacation/beach/photo.jpg` → 提取标签 `vacation`, `beach`（排除日期目录 `2024-01-15`）
- 重复的图片也要将图片所在目录分割为合适的标签保存到现存图片中

### 设计要点

#### 1. 目录名过滤与标签提取规则

| 规则 | 说明 | 示例 |
|------|------|------|
| 排除纯日期格式目录 | 匹配常见日期模式（YYYY-MM-DD, YYYYMMDD 等） | `2024-01-15`, `20240115` |
| **提取混合格式目录的文字部分** | **日期+文字混合，提取文字作为标签** | **`20250414_青岛` → `青岛`, `2025-青岛` → `青岛`** |
| 排除隐藏目录 | 以 `.` 开头的目录 | `.thumbnails`, `.cache` |
| 排除特殊目录 | 特定系统目录或临时目录 | `__pycache__`, `node_modules` |
| 保留有效标签 | 其他所有目录名作为标签 | `vacation`, `beach`, `family` |

**混合格式支持**：
- `20250414_青岛` → 提取 `青岛`（下划线分隔）
- `2025-青岛` → 提取 `青岛`（短横线分隔）
- `2025_01_青岛` → 提取 `青岛`
- `2025年青岛` → 提取 `青岛`（中文日期前缀）
- `青岛_2025` → 提取 `青岛`（文字在前）
- **`2025青岛` → 提取 `青岛`（数字+汉字直接相连）**
- **`青岛2025` → 提取 `青岛`（汉字+数字直接相连）**
- **`2025旅游` → 提取 `旅游`（数字+汉字）**
- **`旅游2025` → 提取 `旅游`（汉字+数字）**

#### 2. 目录名处理与标签提取

```python
import re
from datetime import datetime

def extract_text_from_mixed_dir(name: str) -> str:
    """从混合格式目录名中提取文字部分
    
    处理场景：
    - 20250414_青岛 → 青岛
    - 2025-青岛 → 青岛
    - 2025_01_青岛 → 青岛
    - 2025年青岛 → 青岛
    - 青岛_2025 → 青岛
    - 2025青岛 → 青岛（数字+汉字直接相连）
    - 青岛2025 → 青岛（汉字+数字直接相连）
    - 2025旅游 → 旅游（数字+汉字）
    - 旅游2025 → 旅游（汉字+数字）
    
    Returns:
        提取的文字标签，如果没有文字部分则返回 None
    """
    if not name:
        return None
    
    # 模式1：日期前缀 + 分隔符 + 文字（如 20250414_青岛、2025-青岛）
    # 匹配：8位数字 + [_-] + 文字，或 4位数字 + [_-] + 文字
    patterns_prefix = [
        r'^\d{8}[_\\-](.+)$',      # 20250414_青岛
        r'^\d{4}[_\\-](.+)$',       # 2025_青岛
        r'^\d{4}年(.+)$',          # 2025年青岛
        r'^\d{4}-\d{2}[_\\-](.+)$', # 2025-01_青岛
    ]
    
    for pattern in patterns_prefix:
        match = re.match(pattern, name)
        if match:
            text = match.group(1).strip()
            if text:
                return sanitize_tag(text)
    
    # 模式2：日期前缀 + 汉字/文字（直接相连，无分隔符）
    # 如：2025青岛、20250414青岛
    # 匹配：4位数字或8位数字 + 汉字开头的内容
    pattern_digit_prefix = r'^(\d{4}|\d{8})([\u4e00-\u9fff].*)$'
    match = re.match(pattern_digit_prefix, name)
    if match:
        text = match.group(2).strip()
        if text:
            return sanitize_tag(text)
    
    # 模式3：文字前缀 + 分隔符 + 日期（如 青岛_2025）
    patterns_suffix = [
        r'^(.+?)[_\\-]\d{4}.*$',   # 青岛_2025、青岛-202504
        r'^(.+?)\d{4}.*$',          # 青岛2025（文字开头后接4位数字）
    ]
    
    for pattern in patterns_suffix:
        match = re.match(pattern, name)
        if match:
            text = match.group(1).strip()
            if text and not text.isdigit():
                return sanitize_tag(text)
    
    # 模式4：文字前缀 + 日期（直接相连，无分隔符）
    # 如：青岛2025、旅游2025
    # 匹配：汉字开头 + 4位数字或8位数字结尾
    pattern_text_prefix = r'^(.*?[\u4e00-\u9fff]+?)\d{4,8}$'
    match = re.match(pattern_text_prefix, name)
    if match:
        text = match.group(1).strip()
        if text and not text.isdigit():
            return sanitize_tag(text)
    
    return None

def is_pure_date_dir(name: str) -> bool:
    """判断目录名是否为纯日期格式（排除混合格式）"""
    # 纯日期格式
    date_patterns = [
        r'^\d{4}-\d{2}-\d{2}$',   # YYYY-MM-DD
        r'^\d{8}$',               # YYYYMMDD
        r'^\d{2}-\d{2}-\d{4}$',   # MM-DD-YYYY
    ]
    
    for pattern in date_patterns:
        if re.match(pattern, name):
            # 验证是否为有效日期
            try:
                if len(name) == 10 and '-' in name:
                    datetime.strptime(name, '%Y-%m-%d')
                elif len(name) == 8 and name.isdigit():
                    datetime.strptime(name, '%Y%m%d')
                return True
            except ValueError:
                pass
    return False

def process_dir_as_tag(name: str) -> str:
    """处理目录名，返回标签或 None
    
    逻辑：
    1. 如果是纯日期目录 → 返回 None（排除）
    2. 如果是混合格式（日期+文字）→ 提取文字部分作为标签
    3. 其他情况 → 整个目录名作为标签
    """
    # 1. 排除隐藏目录
    if name.startswith('.'):
        return None
    
    # 2. 排除系统目录
    if name in ('__pycache__', 'node_modules', '.git'):
        return None
    
    # 3. 检查是否为纯日期目录
    if is_pure_date_dir(name):
        return None
    
    # 4. 检查是否为混合格式（日期+文字），提取文字部分
    text_from_mixed = extract_text_from_mixed_dir(name)
    if text_from_mixed:
        return text_from_mixed
    
    # 5. 普通目录名，直接作为标签
    return sanitize_tag(name)
```

#### 3. 标签提取流程（更新版）

```python
from pathlib import Path

def extract_tags_from_path(file_path: str, import_root: str = None) -> list[str]:
    """从文件路径提取标签（智能处理混合格式目录名）
    
    Args:
        file_path: 照片文件路径
        import_root: 导入根目录（可选，用于计算相对路径）
        
    Returns:
        标签列表，如 ['青岛', 'vacation', 'beach']
    """
    p = Path(file_path)
    
    # 如果提供了 import_root，计算相对路径
    if import_root:
        try:
            rel_path = p.relative_to(import_root)
            dir_parts = list(rel_path.parent.parts)
        except ValueError:
            dir_parts = list(p.parent.parts)
    else:
        dir_parts = list(p.parent.parts)
    
    # 处理每个目录部分
    tags = []
    seen = set()  # 去重
    
    for part in dir_parts:
        tag = process_dir_as_tag(part)
        if tag and tag not in seen:
            tags.append(tag)
            seen.add(tag)
    
    return tags

def sanitize_tag(name: str) -> str:
    """清理标签名，转为合适格式"""
    import re
    # 转小写（中文保持不变）
    tag = name.lower()
    # 替换空格和特殊字符为下划线
    tag = re.sub(r'[^\w\u4e00-\u9fff]', '_', tag)
    # 合并多个下划线
    tag = re.sub(r'_+', '_', tag)
    # 去除首尾下划线
    tag = tag.strip('_')
    return tag if tag else None
```

#### 4. 示例测试

| 目录结构 | 提取的标签 |
|----------|----------|
| `20250414_青岛/photo.jpg` | `青岛` |
| `2025-青岛/vacation/photo.jpg` | `青岛`, `vacation` |
| `2025年青岛/beach/photo.jpg` | `青岛`, `beach` |
| `2024-01-15/vacation/beach/photo.jpg` | `vacation`, `beach` |
| `青岛_2025/photo.jpg` | `青岛` |
| `travel/20250414_青岛/photo.jpg` | `travel`, `青岛` |
| `2024-01-15/photo.jpg` | （无标签，纯日期目录被排除）|
| `vacation/beach/photo.jpg` | `vacation`, `beach` |
| **`2025青岛/photo.jpg`** | **`青岛`（数字+汉字直接相连）** |
| **`青岛2025/photo.jpg`** | **`青岛`（汉字+数字直接相连）** |
| **`2025旅游/photo.jpg`** | **`旅游`（数字+汉字）** |
| **`旅游2025/photo.jpg`** | **`旅游`（汉字+数字）** |
| **`20250414青岛/photo.jpg`** | **`青岛`（8位数字+汉字）** |
| **`青岛20250414/photo.jpg`** | **`青岛`（汉字+8位数字）** |

#### 4. 数据库操作

**TagRepo 新增方法**（在 `photo_repo.py` 中）：

```python
class TagRepo:
    def get_or_create(self, name: str, color: str = '#808080') -> int:
        """获取或创建标签，返回标签 ID"""
        # 先查找
        existing = self.get_by_name(name)
        if existing:
            return existing['id']
        # 不存在则创建
        return self.create(name, color)
```

**PhotoRepo 新增方法**：

```python
class PhotoRepo:
    def add_tags_from_path(self, photo_id: int, file_path: str, import_root: str = None):
        """从文件路径提取标签并关联到照片"""
        tags = extract_tags_from_path(file_path, import_root)
        for tag_name in tags:
            tag_id = self.tag_repo.get_or_create(tag_name)
            self.tag_repo.add_photo_tag(photo_id, tag_id)
```

#### 5. 集成到导入流程

**修改 `organizer.py` 的 `_process_file()` 方法**：

```python
def _process_file(self, file_path: Path):
    # ... 现有处理逻辑 ...
    
    # 保存照片到数据库后
    if self.photo_repo and photo_id:
        # 从文件路径提取标签并关联
        self.photo_repo.add_tags_from_path(
            photo_id, 
            str(file_path), 
            str(self.source_dir)  # 导入根目录
        )
    
    # ... 其余逻辑 ...
```

**处理重复照片时也要添加标签**：

```python
# 在 hash_checker.check() 返回 'skipped' 时
if action == 'skipped' and existing_path:
    # 为已存在的照片也添加来自新路径的标签
    if self.photo_repo:
        existing_photo = self.photo_repo.get_by_hash(file_hash)
        if existing_photo:
            self.photo_repo.add_tags_from_path(
                existing_photo['id'],
                str(file_path),
                str(self.source_dir)
            )
```

#### 6. 目录分隔符说明

| 系统 | 分隔符 | 示例 |
|------|--------|------|
| Windows | `\` | `2024-01-15\vacation\beach\photo.jpg` |
| Linux/Mac | `/` | `2024-01-15/vacation/beach/photo.jpg` |

`pathlib.Path` 会自动处理不同系统的分隔符，无需手动处理。

### 实现步骤

1. **添加工具函数** (`python/src/path_utils.py`)
   - `is_date_dir(name)` - 检测日期目录
   - `sanitize_tag(name)` - 清理标签名
   - `extract_tags_from_path(file_path, import_root)` - 提取标签

2. **扩展 TagRepo** (`python/src/photo_repo.py`)
   - 添加 `get_or_create(name)` 方法

3. **扩展 PhotoRepo** (`python/src/photo_repo.py`)
   - 添加 `add_tags_from_path(photo_id, file_path, import_root)` 方法

4. **修改 Organizer** (`python/src/organizer.py`)
   - 在 `_process_file()` 中添加标签提取逻辑
   - 在重复文件处理时也为已存在照片添加标签

5. **测试场景**
   - 新导入照片：验证标签正确提取
   - 重复照片：验证标签添加到已存在照片
   - 嵌套目录：`a/b/c/photo.jpg` → 标签 `a`, `b`, `c`
   - 日期目录：`2024-01-15/photo.jpg` → 无标签（排除日期）
   - 混合场景：`2024-01-15/vacation/beach/photo.jpg` → 标签 `vacation`, `beach`

### 关键决策

| 决策 | 理由 |
|------|------|
| 使用 pathlib 处理路径 | 跨平台兼容，自动处理分隔符 |
| 排除日期目录 | 避免将日期作为标签（日期已有专用字段） |
| get_or_create 模式 | 避免重复创建标签 |
| 重复照片也添加标签 | 确保从不同路径导入都能获得标签 |

---

### 关键决策总结

| 需求 | 推荐方案 | 依赖 | 工作量 |
|------|----------|------|--------|
| 搜索过滤 | 扩展 SQL WHERE (file_name, camera, taken_at) | 无 | 小 |
| Web 入口 | shell.openExternal + 自定义协议(可选) | 无 | 中 |
| 人脸检测 | face-api.js (TinyFaceDetector) | face-api.js | 大 |

---

### 日志模块 (Logger)

#### 日志级别定义

| 级别 | 数值 | 用途 | 输出示例 |
|------|------|------|----------|
| TRACE | 5 | 最详细（函数入口、参数） | `>>> enter organize()` |
| DEBUG | 10 | 开发调试（变量值、流程） | `file_hash = abc123` |
| INFO | 20 | 正常流程 | `Organized 100 photos` |
| SUCCESS | 25 | 成功操作 | `Photo saved: IMG_001.jpg` |
| WARNING | 30 | 警告（可继续） | `Missing EXIF, using mtime` |
| ERROR | 40 | 错误（操作失败） | `Failed to read file` |
| FATAL | 50 | 致命错误（程序终止） | `Database connection failed` |

#### 日志格式

```
{TIMESTAMP} | {LEVEL:^8} | {SOURCE:^15} | {MESSAGE} | {EXTRA}

示例:
2026-04-04 20:30:15 | TRACE    | organizer.py  | >>> enter organize(source=/path, dest=/out)
2026-04-04 20:30:15 | DEBUG    | exif_parser   | EXIF DateTimeOriginal: 2024:06:15 14:30:00
2026-04-04 20:30:15 | INFO     | organizer     | Organized 1/100 photos
2026-04-04 20:30:15 | WARNING  | exif_parser   | Missing EXIF, fallback to file mtime
2026-04-04 20:30:15 | ERROR    | database.py   | SQL Error: UNIQUE constraint failed
```

#### Python 后端日志 (loguru)

```python
# python/src/logger.py
from loguru import logger
import sys
from pathlib import Path

class Logger:
    _instance = None
    _log_level = "INFO"  # 默认级别
    
    @classmethod
    def init(cls, log_dir: str, level: str = "INFO"):
        """初始化日志器
        
        Args:
            log_dir: 日志文件目录
            level: 日志级别 (TRACE/DEBUG/INFO/SUCCESS/WARNING/ERROR)
        """
        cls._log_level = level.upper()
        log_file = Path(log_dir) / "photo-manager.log"
        
        # 移除默认 handler
        logger.remove()
        
        # 控制台输出（带颜色）
        logger.add(
            sys.stderr,
            level=cls._log_level,
            format="<green>{time:YYYY-MM-DD HH:mm:ss}</green> | <level>{level:^8}</level> | <cyan>{name}</cyan>.<cyan>{function}</cyan>:<cyan>{line}</cyan> | <level>{message}</level>",
            colorize=True
        )
        
        # 文件输出（完整日志）
        logger.add(
            log_file,
            level="TRACE",  # 文件记录所有级别
            rotation="10 MB",
            retention="7 days",
            format="{time:YYYY-MM-DD HH:mm:ss.SSS} | {level:^8} | {name}:{function}:{line} | {message}",
            encoding="utf-8"
        )
        
        # 错误日志单独文件
        error_file = Path(log_dir) / "error.log"
        logger.add(
            error_file,
            level="ERROR",
            format="{time:YYYY-MM-DD HH:mm:ss.SSS} | {level:^8} | {name}:{function}:{line} | {message}\n{exception}",
            encoding="utf-8"
        )
    
    @classmethod
    def set_level(cls, level: str):
        """动态设置日志级别"""
        cls._log_level = level.upper()
        # 通过环境变量传递给子进程
        import os
        os.environ["LOG_LEVEL"] = cls._log_level
    
    @classmethod
    def get_logger(cls, module: str):
        """获取模块日志器"""
        return logger.bind(name=module)

# 使用示例
log = Logger.get_logger(__name__)

log.trace("Entering function with args:", {"arg1": value1})
log.debug("Variable state:", {"hash": file_hash})
log.info("Processing photo:", {"path": file_path})
log.success("Operation completed:", {"count": count})
log.warning("Fallback triggered:", {"reason": "missing exif"})
log.error("Operation failed:", {"error": str(e)})
log.exception("Fatal error:", e)  # 自动包含 traceback
```

#### Electron 主进程日志 (electron-log)

```typescript
// electron/src/main/logger.ts
import log from 'electron-log';
import { app } from 'electron';
import path from 'path';

export class Logger {
  private static logLevel: string = 'info';
  
  static init(logDir?: string): void {
    const userDataPath = app?.getPath('userData') || process.cwd();
    const logPath = logDir || path.join(userDataPath, 'logs');
    
    // 配置文件日志
    log.transports.file.resolvePathFn = () => path.join(logPath, 'main.log');
    log.transports.file.level = 'trace';  // 文件记录所有
    log.transports.file.format = '[{y}-{m}-{d} {h}:{i}:{s}.{ms}] [{level}] {text}';
    log.transports.file.maxSize = 10 * 1024 * 1024; // 10MB
    
    // 错误日志单独文件
    log.transports.file.getFile().resolvePathFn = () => path.join(logPath, 'error.log');
    log.transports.error.level = 'error';
    
    // 控制台日志
    log.transports.console.level = this.logLevel;
    log.transports.console.format = '[{h}:{i}:{s}] [{level}] {text}';
    
    // 设置级别
    this.setLevel(this.logLevel);
    
    log.info('Logger initialized', { logPath });
  }
  
  static setLevel(level: string): void {
    this.logLevel = level.toLowerCase();
    log.transports.console.level = this.logLevel;
  }
  
  static trace(message: string, data?: object): void {
    log.transports.console.level = 'trace';
    log.info(`[TRACE] ${message}`, data || '');
  }
  
  static debug(message: string, data?: object): void {
    log.debug(message, data || '');
  }
  
  static info(message: string, data?: object): void {
    log.info(message, data || '');
  }
  
  static warn(message: string, data?: object): void {
    log.warn(message, data || '');
  }
  
  static error(message: string, error?: Error): void {
    log.error(message, error?.stack || error?.message || '');
  }
  
  static verbose(): string {
    return 'electron-log';
  }
}
```

#### React 前端日志

```typescript
// electron/src/renderer/src/utils/logger.ts
type LogLevel = 'trace' | 'debug' | 'info' | 'warn' | 'error';

interface LogEntry {
  timestamp: string;
  level: LogLevel;
  source: string;
  message: string;
  data?: object;
}

class FrontendLogger {
  private static logs: LogEntry[] = [];
  private static maxLogs = 1000;
  private static level: LogLevel = 'info';
  
  static init(level: LogLevel = 'info'): void {
    this.level = level;
    this.info('[Logger] Frontend logger initialized', { level });
  }
  
  static setLevel(level: LogLevel): void {
    this.level = level;
  }
  
  private static shouldLog(level: LogLevel): boolean {
    const levels: Record<LogLevel, number> = {
      trace: 0, debug: 1, info: 2, warn: 3, error: 4
    };
    return levels[level] >= levels[this.level];
  }
  
  private static log(level: LogLevel, source: string, message: string, data?: object): void {
    if (!this.shouldLog(level)) return;
    
    const entry: LogEntry = {
      timestamp: new Date().toISOString(),
      level,
      source,
      message,
      data
    };
    
    this.logs.push(entry);
    if (this.logs.length > this.maxLogs) {
      this.logs.shift();
    }
    
    const prefix = `[${level.toUpperCase()}] ${source}`;
    const args = data ? [prefix, message, data] : [prefix, message];
    
    switch (level) {
      case 'trace':
      case 'debug':
        console.debug(...args);
        break;
      case 'info':
        console.info(...args);
        break;
      case 'warn':
        console.warn(...args);
        break;
      case 'error':
        console.error(...args);
        break;
    }
    
    // 可选：发送到主进程（记录到文件）
    if (window.electronAPI?.logToFile) {
      window.electronAPI.logToFile(entry);
    }
  }
  
  static trace(source: string, message: string, data?: object): void {
    this.log('trace', source, message, data);
  }
  
  static debug(source: string, message: string, data?: object): void {
    this.log('debug', source, message, data);
  }
  
  static info(source: string, message: string, data?: object): void {
    this.log('info', source, message, data);
  }
  
  static warn(source: string, message: string, data?: object): void {
    this.log('warn', source, message, data);
  }
  
  static error(source: string, message: string, error?: Error): void {
    this.log('error', source, message, { error: error?.message, stack: error?.stack });
  }
  
  static getLogs(): LogEntry[] {
    return [...this.logs];
  }
  
  static clearLogs(): void {
    this.logs = [];
  }
}

export const logger = FrontendLogger;
```

#### IPC 日志传递

```
┌─────────────────────────────────────────────────────────────────┐
│                     日志流向图                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   React 渲染进程                                                │
│   ┌───────────────────────────────────────────────────────┐     │
│   │ logger.info('Gallery', 'Loading photos', {count: 100}) │     │
│   └───────────────────────┬───────────────────────────────┘     │
│                           │ IPC (发送到主进程)                   │
│   ┌───────────────────────▼───────────────────────────────┐     │
│   │ Electron 主进程                                        │     │
│   │ ┌─────────────────────────────────────────────────┐   │     │
│   │ │ logger.info('IPC', 'query-photos invoked')      │   │     │
│   │ └───────────────────────┬─────────────────────────┘   │     │
│   │                         │ IPC (转发到 Python)            │     │
│   │   ┌─────────────────────▼─────────────────────────┐ │     │
│   │   │ Python 子进程                                  │ │     │
│   │   │ log.info("Executing query", {"filters": {}})   │ │     │
│   │   │ log.info("Query completed", {"count": 100})    │ │     │
│   │   └─────────────────────┬─────────────────────────┘ │     │
│   │                         │ stdout JSON 日志             │     │
│   │   ┌─────────────────────▼─────────────────────────┐ │     │
│   │   │ 主进程解析并写入日志文件                         │ │     │
│   │   └─────────────────────────────────────────────────┘ │     │
│   └───────────────────────────────────────────────────────┘     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 日志级别控制

```bash
# 启动时指定日志级别
photo-manager --log-level DEBUG

# 开发模式（详细）
npm run dev -- --log-level trace

# 生产模式（仅重要信息）
npm run build && photo-manager --log-level info

# 设置界面
```

```typescript
// Settings 页面支持动态调整
const handleLevelChange = (level: string) => {
  // 通知主进程
  window.electronAPI.setLogLevel(level);
  // 更新前端
  logger.setLevel(level as LogLevel);
};
```

### Python 后端模块

| 模块 | 职责 | 公开 API |
|------|------|----------|
| `organizer.py` | 扫描、解析时间、整理文件 | `organize(source, dest, mode)` |
| `database.py` | SQLite CRUD、索引 | `PhotoRepo` 类 |
| `exif_parser.py` | EXIF 读取、时间解析 | `parse_exif(path)` |
| `face_detector.py` | 人脸检测、特征提取 | `detect_faces(path)` |
| `json_exporter.py` | JSON 导入/导出 | `export_db()`, `import_db()` |
| `logger.py` | 日志管理 | `Logger.init()`, `Logger.get_logger()` |

### 前端页面

| 页面 | 路由 | 功能 |
|------|------|------|
| Gallery | `/` | 照片网格浏览、筛选、搜索 |
| Organizer | `/organize` | 整理向导、进度显示 |
| Annotation | `/photo/:id` | 单张照片标注、人脸命名 |
| Settings | `/settings` | 整理规则、数据库路径 |

---

## 设计原则

### 1. 表命名

```
{模块}_{内容描述}

规则：
- 模块前缀：photo / sys
- 内容描述：用下划线分隔，体现业务含义
- 允许多段式命名

示例：
photo_info              → 照片信息
photo_face_detect       → 人脸检测
photo_face_subject      → 人物主题
photo_photo_tag         → 照片标签关联
sys_setting             → 系统设置
sys_log_operation       → 操作日志
```

### 2. 字段命名

```
snake_case（小写下划线）

规则：
- 全部小写
- 单词间用下划线
- 时间字段用 _at 后缀
- 布尔字段用 is_ / has_ 前缀

示例：
file_path, taken_at, is_portrait, created_at
```

### 3. 公共字段

所有表必须包含：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | INTEGER | 主键，自增 |
| `created_at` | DATETIME | 创建时间 |

---

| 决策 | 原因 | 备选方案 |
|------|------|----------|
| Electron + Python | 参考 fin-agent-desktop，兼顾桌面体验和 Python 生态 | 纯 PyQt / 纯 Web |
| SQLite 存储 | 轻量、无依赖、支持 SQL 查询 | PostgreSQL, MySQL |
| JSON 导出 | 保证数据可迁移性 | 仅数据库 |
| electron-vite | 参考 fin-agent-desktop，开发体验好 | electron-forge |
| Tailwind CSS | 快速开发、参考项目已用 | styled-components |
| 时间 fallback | 兼容无 EXIF 的截图/网图 | 强制要求 EXIF |

---

## 数据模型

### 表设计

详细表结构设计见：[数据库设计.md](./数据库设计.md)

**核心表概览**：

| 表名 | 模块 | 说明 |
|------|------|------|
| `photo_info` | photo | 照片信息 |
| `photo_tag` | photo | 标签 |
| `photo_photo_tag` | photo | 照片-标签关联 |
| `photo_face_detect` | photo | 人脸检测 |
| `photo_face_subject` | photo | 人物主题 |
| `photo_log_operation` | photo | 操作日志 |
| `sys_setting` | sys | 系统设置 |
| `sys_log_operation` | sys | 操作日志 |

**ER 关系**：
```
photo 模块:
photo_tag ─── photo_photo_tag ─── photo_info ─── photo_face_detect
                                                     │
                                          photo_face_subject

photo_log_operation (独立日志)

sys 模块:
sys_setting ─── sys_log_operation
```

---

## 开发命令

```bash
# Electron 前端开发
cd electron
npm install
npm run dev          # 开发模式

# Python 后端
cd python
pip install -r requirements.txt
python src/main.py --help

# 打包
npm run build        # 构建
npm run build:win    # 打包 Windows
```

---

## 参考项目

| 项目 | 用途 |
|------|------|
| [fin-agent-desktop](https://github.com/YUHAI0/fin-agent-desktop) | Electron + Python 架构参考 |
| [PhotoPrism](https://github.com/photoprism/photoprism) | 功能设计参考 |
| [electron-vite](https://electron-vite.org/) | 构建工具文档 |

---

*文档版本: v0.7*
*更新日期: 2026-04-13*
*更新内容: 
- 新增导入记录功能（import_record, import_item 表）
- Hash 去重机制：跳过（记录）或覆盖
- 直接操作模式，无需预扫描
*基于: fin-agent-desktop 架构*
